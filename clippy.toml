# Global Clippy configuration for rez-core
# This file provides unified clippy settings across the entire project

# Cognitive complexity threshold
cognitive-complexity-threshold = 30

# Type complexity threshold
type-complexity-threshold = 250

# Too many arguments threshold
too-many-arguments-threshold = 8

# Too many lines threshold
too-many-lines-threshold = 150

# Single char binding names threshold
single-char-binding-names-threshold = 4

# Avoid false positives for large types
pass-by-value-size-limit = 256

# Minimum supported Rust version
msrv = "1.70.0"

# Allow some lints in tests (tests can use unwrap/print for simplicity)
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests = true
allow-print-in-tests = true

# Note: To deny unwrap_used in production code, add to each crate's lib.rs or main.rs:
#   #![deny(clippy::unwrap_used)]
# For print statements in production code, use:
#   #![warn(clippy::print_stdout)]
# These are not clippy.toml fields but code attributes.
