//! Tests for VersionToken compatibility with rez
//!
//! Note: VersionToken tests for the `version_token` module were removed
//! because the module is a pyo3-only Python binding (used by rez-next-python).
//! The core Rust version token type lives in `token.rs`.
